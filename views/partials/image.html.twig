{#
 # Image
 #
 # @param array classes
 # @param string loading
 # @param array image
 # @param array attributes
 # @param string size Optional. The requested image size. This can be a size that was in WordPress. Example: medium or large. Default full.
 #
 # @package WordPress
 # @subpackage Rider404
 #}
{% if image %}

	{% set image = get_image( image ) %}

	<img
		class="{{ classes ? classes | join( ' ' ) }}"
		loading="{{ loading | default( 'lazy' ) }}"
		src="{{ image.src( size | default( 'full' ) ) }}"
		alt="{{ image.alt }}"
		width="{{ image.width }}"
		height="{{ image.height }}"
		srcset="{{ image.srcset }}"
		sizes="{{ image.img_sizes }}"
		{{ attributes ? attributes | map( ( value, key ) => "#{key}=\"#{value}\"" ) | join( ' ' )  | raw }}
	>
{% endif %}

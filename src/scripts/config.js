const elements = {
	body: document.body,
	html: document.documentElement,
};

const breakpoints = {
	xs: 0,
	sm: 414,
	md: 1440,
	lg: 1600,
	hd: 2560,
};

const classes = {
	active: 'is-active',
	inview: 'is-in-view',
	select: 'is-selected',
};

const scroll = {
	left: 0,
	top: 0,
};

export { elements, breakpoints, classes, scroll };
